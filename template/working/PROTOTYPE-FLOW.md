# PROTOTYPE-FLOW.md
> Working doc — build flow. Owned by the Designer role.
> The screen-by-screen sequence the prototype must implement and the proof it must deliver.
> Build logic only. Do NOT put changelog, status, or critique here — those live elsewhere.

---

## The scenario
> The concrete story the prototype walks through. One named user, one clear goal, the steps
> they take. A specific scenario keeps the AI building one coherent flow instead of a pile
> of disconnected screens.

[user] is a [role]. Their goal today is to:
1. [step]
2. [step]
3. [step]

**Important constraint:** [the real-world rule the flow must respect]

---

## Prototype rule
> The core "if this, then that" the prototype demonstrates. State it plainly.

Once [the key action] happens:
- [resulting behavior]
- [resulting behavior]

---

## Flow A — [name]
> Each screen: what the user sees, what they do, what the prototype must show. Note the
> critical decision points explicitly.

### Screen 1 — [name]
- User sees: [...]
- User does: [...]
- Prototype must show: [...]

### Screen 2 — [name]
...

---

## Flow B — [name, e.g. verification flow]
> Keep setup flow and verification flow separate (see ROLE-GUIDE reusable patterns).

### Screen N — [name]
...

---

## What the prototype must demonstrate
> The proof checklist. Group by type so you can verify nothing core is missing.

### Core proof
- [the central thing the prototype proves]

### UX proof
- [the experience holds up — findable, understandable, native-feeling]

### Product proof
- [the strategic logic holds — extends existing patterns, respects constraints]
